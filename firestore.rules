
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write access to all collections for now.
    // We will add security rules later.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
